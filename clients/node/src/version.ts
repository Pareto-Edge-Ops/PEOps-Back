// Single source for the SDK version. Kept in lockstep with the Python client
// (clients/python/pyproject.toml) so the dashboard's "peops-sdk v{version}" chip
// describes both clients truthfully.
export const VERSION = "0.2.0";
